{
  "name": "nasa-mirror-api",
  "version": "0.1.0",
  "description": "Cloudflare Workers API for NASA APOD mirror scraping",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "cargo build --release",
    "test": "cargo test"
  },
  "devDependencies": {
    "wrangler": "^4.70.0"
  }
}
