package space.o4bit.projectasteria.ui.theme

import androidx.compose.ui.graphics.Color

// Dark mode colors (primary palette)
internal val DeepSpace = Color(0xFF0A1128) // Deep space background
internal val SpaceBlack = Color(0xFF000814) // Deep black for space backgrounds
internal val StardustSilver = Color(0xFFCCD6F6) // Silvery color for text/details
internal val NebulaViolet = Color(0xFF9C27B0) // Violet color for nebula elements
internal val CosmicAccent = Color(0xFF00B0FF) // Bright cosmic accent

// Additional color options available for future use
// Remove these completely or add them back as needed for new UI elements
