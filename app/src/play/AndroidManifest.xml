<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Firebase Messaging Service - Only for Play builds -->
        <service
            android:name=".data.notification.SpaceMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Debug Activity for Crashlytics Testing - Only for Play builds -->
        <activity
            android:name=".TestCrashActivity"
            android:exported="false"
            android:label="Test Crashlytics"
            android:theme="@style/Theme.ProjectAsteria" />
    </application>

</manifest>
